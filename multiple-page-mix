Role: Principal Frontend Architect & Clean Code Expert
Task: Orchestrate and Refactor Scattered Designer-Generated (Figma) Components into a Standard, Production-Ready React Feature Module

Context:
I am importing a raw, decoupled code snippet package generated by a design-to-code tool. It contains:
1. One global visual layout blueprint (index.html)
2. A main data-presentation component (Main/List View Component)
3. A secondary administrative/configuration view component (Detail/Admin View Component)

Goal:
Analyze the structural topology, behavioral affinity, and layout tree of these files. Refactor and blend them into a highly robust, loosely coupled, and responsive top-level feature container that follows standard React architectural patterns.

---
【CONFIGURATION VARIABLES】
- Feature Module Name (PascalCase): [e.g., OrderManagement / UserProfile]
- Target Programming Language: [TypeScript / JavaScript]
- Unified Style System: [e.g., Tailwind CSS / CSS Modules / Styled-components]
---

Please strictly adhere to the following universal technical specifications during the refactoring process:

1. CONTAINER-PRESENTER PATTERN & ORCHESTRATION
- Generate a top-level orchestrator named exactly `[Feature Module Name]Container.tsx`. This container acts as the "Smart Component" and must maintain the state machine for view navigation (e.g., `currentView: 'LIST' | 'DETAIL'`) and contextual data payload (e.g., `selectedId: string | null`).
- Parse `index.html` to isolate the persistent "Application Shell" (e.g., Global Header, Sidebar navigation) and establish it as the static layout frame of the container component.
- Extract the dynamic "Viewport Area". Within the container, this viewport must dynamically hot-swap between the Main/List View and Secondary/Detail View using declarative conditional rendering based on the `currentView` state.

2. STATE LIFTING & REVERSE DATA FLOW (Inversion of Control)
- Maintain the purity of the sub-views as "Dumb/Presenter Components." Do not embed cross-view navigation or global side-effects inside them.
- Inject explicit inversion-of-control callbacks via Props to enable unidirectional data flow:
  * Main/List View Component: Must expose an item selection callback (e.g., `onItemSelect(id: string): void`) triggered by clicking the primary anchor element (e.g., Case ID, Row Item) to lift the data and trigger view-switching.
  * Secondary/Detail View Component: Must expose a reverse navigation callback (e.g., `onBack(): void`) mapped to the back/close button to ensure a graceful return flow to the main view.
- Provide strict type definitions (TypeScript Interfaces or PropTypes) for the refactored props of all modified files.

3. LAYOUT LIQUIDITY & STYLING HYGIENE
- Completely eradicate all absolute positioning metrics (`position: absolute`, hardcoded `top`/`left` pixel offsets) inherited from the design canvas coordinate space.
- Re-architect all core wrapper wrappers using standard CSS Grid or Flexbox alignment to guarantee 100% responsive fluid behavior across fluid viewport sizes.
- Extract all hardcoded strings, asset URLs, and static highlight states into dynamic, configurable properties or Props.

Expected Output Deliverables:
1. The recommended [Standard Directory Tree Structure] for this feature module.
2. The complete production-ready source code for the new `[Feature Module Name]Container.tsx` component.
3. Precise, block-by-block [Code Diff Instructions] showing exactly how to update the existing sub-components to seamlessly integrate with the new Props interface.

---
【SOURCE FILES FOR ANALYSIS AND COMBINATION:】

【File 1: Global Visual Reference - index.html】
[PASTE OR UPLOAD YOUR HTML HERE]

【File 2: Main/List View Component - Source Code】
[PASTE OR UPLOAD YOUR LIST VIEW CODE HERE]

【File 3: Secondary/Detail View Component - Source Code】
[PASTE OR UPLOAD YOUR DETAIL VIEW CODE HERE]
